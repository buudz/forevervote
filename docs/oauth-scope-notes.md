# OAuth scope notes

Current requested scopes:

- `openid`
- `wow.profile`

The goal is to identify the Battle.net account and inspect the user's WoW account profile summary. Do not request broader scopes unless a specific launch feature requires them.
