# Temporary auth status

Auth is currently an inspection-only step. Users can log in and the app can show the BattleTag plus sanitized profile checks.

Voting remains disabled until:

- Supabase persistence is wired.
- Classic eligibility is confirmed.
- Vote writes are implemented server-side.
