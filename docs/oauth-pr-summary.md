# OAuth PR summary

Adds the first Battle.net login test flow:

- Login redirect route
- Callback route
- OAuth state validation
- Token exchange server-side
- Battle.net userinfo fetch
- WoW namespace profile checks
- Signed HttpOnly session cookie
- Logout route
- `/api/auth/me` endpoint
- Header login status UI

This PR does not enable voting.
